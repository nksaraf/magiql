schema: schema.graphql
documents:
  - "test/**/*.graphql"
  - "test/**/*.{ts,tsx}"
extensions:
  endpoints:
    default: https://graphql-pokemon.now.sh
  codegen:
    scalars:
      DateTime: Date
    overwrite: true
